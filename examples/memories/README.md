# Example memories

Drop these into `~/.claude-memory/` (or your configured directory) to see how memories look on disk and how scopes interact with retrieval.

These are *examples*, written by hand to illustrate format, not for you to actually rely on. Either delete them or write your own once you are set up.
