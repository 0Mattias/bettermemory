# W2 body census — the read and the park: richer everywhere, empty in the same place

The census `bench/w/W2_BODY_CENSUS_DECLARATION.md` declared ran whole
(`bench/w/results/w2-body-census-2026-08-20.json`), inside budget
(147.8 seconds of the declared 30 minutes), and the fenced ladder's
bottom row fires again: **zero of the committed six prize pairs are
supported on the body surface — PARK. The labeled-duplicate corpus in
hand is closed on BOTH its surfaces, and W2 entry on this corpus
closes whole.**

## The read

Over 37,502 resolved pair bodies (66 more than the title pass
resolved — edges whose titles cleaned to identical strings resolve as
distinct bodies), 22,950 of them in the consumer-tech register, in
engine stem space, by W3-P2's exclusive-substitution rule unchanged:

- **The committed six: 0 of 6 supported.** toggle/flag 0,
  timeout/expiry 0, flag/boolean 0; undo/rollback 1 and
  revert/rollback 1; error/exception 4 — the closest approach, one
  below the family's evidentiary minimum, and even a hit there would
  read one-of-six, still inside the park band. Under the twitch
  band, so the Stack Overflow fetch that band would have licensed
  stays unlicensed under this declaration too.
- **The markup-text variant matches the prose surface on every
  non-zero row** (1, 1, 4). The cleaning rule deleted nothing that
  mattered: the zero is not an artifact of stripping code and
  quotes, and vocabulary living only in code spans was not where the
  prize was hiding.
- **The expanded family: 6 of 45 supported** — backup/restore (42),
  remove/removal (12), library/package (7), rip/remove (5),
  dump/backup (5), old/legacy (5). Every one is the substitutability
  class the trained geometries already carry
  (`bench/w/results/w1c-geometry-2026-08-20.json`): the body surface
  is three times richer than the title surface where skip-gram
  already sees, and exactly as empty where it is blind.
- **The continuity row** moves the way bodies should: preference
  needs that read single digits on titles read tens on bodies
  (09d032c9 at 67, 75832dbd at 129), while two needs stay at zero.
  Informational — the preference routes closed at W3-C and stay
  closed; the row exists so the two censuses read on one page.

## The zero is real — and this time the marginals are fat

The declaration built the presence check into the artifact rather
than running it after the fact, and it lands the sharpest fact of the
unit. Counting kept pair-bodies whose prose surface carries each
probe stem:

```
toggle 150   flag 190    undo 102     rollback 21   revert 173
timeout 49   expiry 100  boolean 5    error 3292    exception 233
(the artifact's presence_bodies_prose block, engine stem space,
 over 2 x 37,502 kept bodies)
```

The title park could still lean on thin marginals — vocabulary in
tens of titles out of tens of thousands leaves the joint event
little room. The body read takes that explanation away: toggle sits
in 150 bodies and flag in 190, yet they NEVER substitute across a
duplicate edge — and never even co-occur on one side. Where both
forms of a prize pair do appear, they appear in the SAME body:
error/exception co-occurs same-side 22 times against 4 exclusive
substitutions. One mechanism survives, and it is the one the W1b
geometry probe named from the other end: askers of one need in these
registers reuse one technical noun on both sides of a duplicate
edge; the cross-form mate, when it appears, is the same author's
elaboration, not a paraphrase's alternative. Co-occurring pairs that
never substitute are exactly what W1c said a W2 objective must
reward — and exactly what pair-positive supervision over these edges
cannot see, because the signal never crosses the edge.

## What the park closes

- The first-named candidate axis of the title park — bodies, not
  titles — priced and dead within the same day it was named. The
  declaration's park text applies as written: the labeled-duplicate
  corpus in hand is closed on both its surfaces.
- With it, the last unpriced surface of the corpus in hand. The
  five-route sweep — static tables, two-pass feedback requery,
  window width, duplicate titles, duplicate bodies — stands measured
  shut end to end, each on an instrument that could see, each with
  its mechanism named. Lane W remains fully parked; the map the
  measurement program finished two units ago is now finished one
  layer deeper.

## What the park opens — named, not licensed

The title park's second candidate is the only mechanical route left
standing, and this record prices its argument in both directions
rather than licensing it:

- **For the programming register:** the presence rows show the prize
  vocabulary is native to software prose and peripheral here —
  boolean in 5 bodies, exception in 233, against error's 3,292.
  Stack Overflow's duplicate edges are the one corpus where these
  words are the working vocabulary rather than borrowed terms.
- **Against it:** the register-reuse mechanism just survived a
  presence check with fat marginals. error/exception is the most
  software-native pair in hand and the densest — and it still reads
  4 exclusive against 22 same-side. A declaration for the fetch must
  argue why programming-register askers would alternate across an
  edge where every register in hand shows authors reusing one form
  and elaborating with the other inside one body.

The standing alternative is unchanged: the interim sentence — the
bars restate to what own-built machinery honestly reaches, and the
published truth stays exactly as measured.

## Owner doors

Opened by the title park, sharpened by this unit: **the W2 data-axis
commission now has one live candidate** — the Stack Overflow
PostLinks fetch under an argued declaration — or the rejection that
stands on the interim sentence. Standing: both bar restatements (the
dev bar's instrument is gone; the LME bar's reference is superseded),
now read against a mechanism program parked on a fully priced corpus.
