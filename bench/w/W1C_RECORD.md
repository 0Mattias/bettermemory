# W1C — the gate read and the park: the objective is the wall, priced

The unit `bench/w/W1C_DECLARATION.md` declared ran whole: one flag
moved (dynamic window 5 → 15, every other byte of the W1b recipe
frozen, same seed), the training ran inside every budget, the derived
census read both windows paired, and the fenced ladder's second row
fires: **OBJECTIVE-IS-THE-WALL — W1b's conclusion is CONFIRMED, and
the last in-family knob is spent.** Nothing ships; a park earns no
ship sentence.

## The training, against its budgets

`/Volumes/data/bettermemory/runs/w1c-2026-08-20` (meta.json beside the
vectors): wall 39,640.3 seconds against the 24-hour ceiling;
196,088,986 tokens read — **the exact W1b count**, confirming the read
stream never changed; 5,110,925,823 pairs against W1b's roughly
1.9 billion, the roughly-threefold update count the declaration's
confound 2 predicted from mean-width scaling.

## The primary read — the census, both windows

Artifact: `bench/w/results/w1c-geometry-2026-08-20.json` (45 expanded
pairs enumerated from `SYNONYM_GROUPS`, 41 in vocabulary; probe
machinery and mutual-rank window unchanged from the committed
`w1b_geometry_probe.py`).

| family | window 5 | window 15 |
| --- | --- | --- |
| morphological | 4/4 | 4/4 |
| cross_form (committed six) | 0/6 | 0/6 |
| near_synonym | 1/5 | 1/5 |
| **cross_form_expanded** | **8/41** | **6/41** |

The wide window bought nothing and cost a little: the expanded family
FELL from 8 to 6 inside the emission window, and every pair inside at
either width is n-gram-adjacent or habitually co-narrated
(backup/restore, staged/stage, switchover/switching, remove/removal)
— the substitutability signature again. The true cross-form bridges
moved the wrong way: toggle/flag reads cosine 0.2533 at window 15
against 0.284 at window 5 (the committed
`w1b-geometry-2026-08-18.json`); flag/boolean stays negative at
mutual ranks near one hundred thousand. Ladder arithmetic: expanded
share 6/41 is under the twenty-percent line, morphological holds at
4/4 — the CONFIRMED row, by more margin than it needed.

The declaration's confound 2 now works FOR the verdict: the window-15
run performed roughly three times the updates on the same read stream
and still lost cross-form ground, so "not enough training" is not an
available objection. Skip-gram's objective learns substitutability at
window 5 and at window 15; co-occurrence relations do not arrive by
widening, they are pushed apart by it.

## The secondary read — expectation met

The syn-form table emitted from the window-15 vectors at W1b's
committed emission settings
(`bench/w/artifacts/w1c_table_2026-08-20.py`, parameters and sha in
the emitter's JSON output recorded in
`bench/w/results/w1c-dev-syn-2026-08-20.json`), graded on the
120-question instrument against the committed expansion-off row
(`bench/retrieval/results/i1-full120-off-2026-08-18.json`), paired by
slug: asked recall reads 0.2583 / 0.5083 against off's 0.2167 /
0.475 — net +5 of 120 at rank one and +4 of 120 at rank five, both
far from significance under the paired exact test on an instrument
whose floor is six questions. The declared expectation ("nothing
measurable") holds; the learned wide-window table joins the hand
tables and the window-5 table in doing no detectable work at this
scale.

## What the park closes, and what it opens

CLOSED: W1b's "STILL OPEN" clause (bench/POWER_AUDIT.md's re-opened
conclusion) — settled. The window axis is spent; within the skip-gram
family there is no remaining hyperparameter with a mechanism-level
claim on the cross-form wall. Static tables (I1), two-pass feedback
requery (census 2), and now window width have each been measured shut
on instruments that can see.

OPEN, and now carrying the whole vocabulary prize: **the W2
contrastive rung**, whose entry decision is an owner door and whose
precondition is the declared pair-class census — does labeled pair
data carry the technical-synonym class, and the preference/paraphrase
class the comparative unit priced? This record adds one design fact
to that census's brief: whatever objective W2 declares must reward
CO-OCCURRING pairs that never substitute, because two units have now
measured that substitutability objectives cannot reach them at any
width.

## Owner doors

None opened by this unit; the standing doors (W2 entry, both bar
restatements, Lane T criterion) are where the forward plan holds
them.
