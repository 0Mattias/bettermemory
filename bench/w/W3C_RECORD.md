# W3-C — the record: parked at the gate, 2026-08-17

The unit ran its full declared ladder — the first Lane W unit to pass
a census — and parked at the gate. The chain is sha-ordered:
declaration with the anatomy re-earned byte-identical (064c4ea),
builder with its CI determinism leg (066622f), the committed table
with B0 published beside it (f4660a1), then the gate invocation set
at that commit. The gate artifacts are
`bench/w/results/gate-w3c-lme-bridge-2026-08-17.json`, its
determinism repeat, the paired incumbent read, the dev read, and the
per-question sidecars under `bench/w/results/per-question/`. Nothing
below renegotiates anything; the bars were fixed at the declaration
commit, and the read fell under three of them.

## B0 and the tuning trail, before any read

The build ran at the default floors and published survival at ONE of
the census's five supported needs
(`bench/w/artifacts/w3c-build-2026-08-17.json`): battery. The two
tuning builds mapped the declared floor space rather than rescuing
it — raising the definitional score floor to 4 changed nothing the
needs could feel (`w3c-build-2026-08-17-d4.json`), and halving the
pair count floor lost battery too as the doubled table reshuffled
PPMI marginals and mutual ranks (`w3c-build-2026-08-17-p5.json`).
The reachable ceiling under the declared neutral rule was one
surviving bridge, and §8.2's arithmetic consequence was on the
record before the gate: G1p out of reach, the unit running for
PARTIAL and for the measurement.

## The gate, read

One invocation set at commit f4660a1, the committed table as the
leg's only vocabulary, the paired incumbent in the same set:

- **The clock control held exactly.** The incumbent arm reproduced
  macro recall@5 0.9062 and recall@1 0.5339
  (`gate-w3c-lme-incumbent-2026-08-17.json`) — the gate's numbers
  are movements, not drift.
- **G1 fails.** The bridge arm read macro recall@5 **0.8635**,
  recall@1 **0.4647** (`gate-w3c-lme-bridge-2026-08-17.json`) —
  4.3 points of macro damage at @5, 6.9 at @1, against a bar that
  required gains.
- **G1p fails at an exact standstill.** Single-session-preference
  @5: **0.7333** in both arms — and the standstill is two real
  movements cancelling, not stillness (next section).
- **G1h fails.** Odd-half macro@5 delta −0.0266
  (per-question sidecars); the even half read −0.0737 — the damage
  is corpus-wide, not a tuning-half artifact.
- **G2 fails on the per-type tripwires.** single-session-assistant
  1.0000 → 0.8929, temporal-reasoning 0.8675 → 0.8118,
  multi-session 0.8663 → 0.8216, single-session-user 0.9714 →
  0.9429 — all far past the 0.5-point guard. The dev floor HELD,
  and above its guard: as-asked r@1 0.35, r@5 **0.65**
  (`gate-w3c-dev-bridge-2026-08-17.json`) against guards of 0.35
  and 0.60.
- **G3 holds.** The repeat reproduced the primary's results block
  and every per-question rank identically; the build repeated over
  the same inputs reproduced the committed table byte-identically
  (only the run JSON's provenance commit stamp moves between
  invocations); the CI leg is committed and green.

**Verdict: W3C-PARK**, per the declared grid — G2 failed, G1 and
G1p failed, and the class net-moved nothing. The engine is
behaviorally unchanged; the composed table remains a bench artifact
riding no default.

## What the gate teaches — both halves of the thesis in one read

1. **The bridge mechanism WORKS where a bridge survives — the
   battery question is the program's first measured preference
   movement.** The one surviving bridge (battery→charging) fired at
   the gate: the battery question's gold session moved from rank 7
   under the incumbent to rank 1 under the bridge arm
   (`per-question/gate-w3c-lme-bridge-pq-2026-08-17.json`, qid
   09d032c9). Three units of census pricing said the relation was
   carried; the gate says a carried relation, riding the leg's
   existing floors, converts to decisive rank movement. The
   mechanism premise of the entire W3 branch is confirmed at the
   question level — once.
2. **And the neutral table costs more than its one bridge earns.**
   The same read moved the guitar question's gold from rank 3 out to
   rank 8 (same sidecars, qid 95228167): the `guitar` head's
   evidence-mass terms — dobro, plectrum, instrument, arpeggione —
   boosted competing sessions past a gold the incumbent already
   ranked. One question in, one question out, class standstill; and
   the same hub vocabulary, firing engine-wide, produced the
   per-type damage G2 caught. The composition did not fail for lack
   of bridges; it failed because a neutral, evidence-mass-shaped
   table buys its bridges with dilution the conversational
   instrument will not pay.
3. **The dev divergence completes W1's polarity lesson from the
   learned side.** The identical table that damaged every
   conversational type lifted the keyword instrument above its
   guard — dev as-asked @5 0.65
   (`bench/w/results/gate-w3c-dev-bridge-2026-08-17.json`) against
   the 0.60 guard
   (`bench/retrieval/results/r1-unpadded-2026-08-13.json`).
   Expansion vocabulary is instrument-polar: what dilutes
   conversational stores enriches keyword recall. That is W1b's
   question, sharpened to a point: the dev bar, not the preference
   class, is where learned vocabulary has now shown measured lift.

## A mishap, disclosed

After the gate set completed, a file-handling error in the session
destroyed the two bridge-arm per-question sidecars (an `rm` on a
directory holding runner outputs that had resolved to a wrong
relative path; the four primary artifacts and the incumbent sidecar
were untouched). The sidecars were regenerated by re-running the
identical invocations; the regenerated results blocks were verified
equal to the committed gate artifacts before use, per-question ranks
identical across the determinism pair. The regeneration is a
reproduction of the same read, not a new read; it is disclosed here
rather than smoothed over.

## What the park licenses, and the program close

Nothing ships and no ship sentence is put. With this read the
preference class's declared non-neural routes are all priced:
paraphrase structure parked at its census, definitional structure
parked at its census, and their composition passed its census, built
its bridge, and parked at the gate with the class at an exact
standstill and the macro damaged. The class stands at 0.7333; the
program's remaining LongMemEval distance remains structurally this
class; and no further route under it is proposed, because the two
honest ones this record could name are both closed by the program's
own doctrine — a need-shaped table is test-fitting by construction,
and hand curation is the thing the W branch exists to replace. The
battery result stands in the record as the mechanism's proof and its
epitaph in one line: the bridge works; the corpus-neutral table that
must carry it does not.

Lane W's queue after this read: **W1b is the whole queue** — the
wide-register vocabulary retrain, aimed at the dev bar, now carrying
two sharpened motivations from today (the situational relation class
lives in running-text co-occurrence; learned vocabulary shows
measured dev lift). Its declaration is owed before any training
byte is read. W2 remains the conditional endgame rung behind it.

## Owner doors

- **Lane T criterion v1** stands where the last record left it.
- Any wiring of any bench table into the package remains a separate
  unit with its own plain sentence; nothing from this unit is a
  candidate.

## What is not claimed

No criterion claim; the interim sentence stands. No generalization
from one moved question to the class — the standstill is the class
result. No claim that different floors reach a different verdict —
the tuning trail is published and the reachable space was mapped.
No W1b entry decision. No comparative claim against any other
system.
