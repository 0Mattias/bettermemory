# W2 SO census — the read and the license: the register alternates

The census `bench/w/W2_SO_CENSUS_DECLARATION.md` declared ran whole,
inside every declared budget (stage 2's pass: 967.0 seconds of the
declared four hours, both sha ceremonies included —
`bench/w/results/w2-so-census-2026-08-20.json`), and for the first
time in this lane a W2-entry ladder resolves to its top row: **five of
the committed six prize pairs are supported on the Stack Overflow body
surface — LICENSE. Writing the W2 training declaration over this
surface is licensed; the entry decision stays downstream and owns the
supervision-mass question.**

## The read

Over 752,352 kept pair bodies from 758,058 deduped duplicate edges
(6,552,590 link rows scanned; 2,209 edges unresolved, 3,497 dropped by
rule), in engine stem space, every counting rule imported unchanged
(`bench/w/results/w2-so-census-2026-08-20.json`):

| pair | exclusive | post-side | related-side | unique targets | same-side | supported |
|---|---|---|---|---|---|---|
| toggle/flag | 13 | 6 | 7 | 8 | 37 | YES |
| undo/rollback | 10 | 2 | 8 | 8 | 20 | YES |
| revert/rollback | 15 | 4 | 11 | 7 | 12 | YES |
| timeout/expiry | 2 | 1 | 1 | 2 | 6 | no |
| flag/boolean | 33 | 11 | 22 | 30 | 136 | YES |
| error/exception | 9,437 | 6,884 | 2,553 | 3,472 | 8,727 | YES |

The expanded family reads 21 of 45 supported. An independent-reader
cross-check rides the artifact: the pass counted 59,819,048 rows with
24,101,803 question rows — exactly the PostTypeId census W3-P's
reader published from this same pinned archive
(`bench/w/results/w3p-census-2026-08-17.json`). Two readers, one
archive, identical row accounting.

## The bands, read against the declared argument

The declaration's band map said the volume null predicts PARK with
error/exception as the plausible lone support. The read falsifies the
null: four pairs beyond error/exception crossed, and the two
surface↔implementation rows the argument turned on — toggle/flag and
flag/boolean — read 13 and 33 here after reading ZERO across all
37,502 in-hand pair bodies (`bench/w/results/w2-body-census-2026-08-20.json`)
and zero again across the 40,103 legacy Stack Overflow titles
(`bench/w/results/w2-so-stage0-2026-08-20.json`). The register the
declaration argued for is the register where they cross.

error/exception carries the directional signature the mechanism
predicted: 6,884 of its 9,437 exclusive crossings put the surface form
on the closed-question side
(`bench/w/results/w2-so-census-2026-08-20.json`), replicating stage
0's 25 of 35 on the title surface. The other supported rows read
near-balanced (6/7, 2/8, 4/11, 11/22): the closure boundary carries
genuine two-way alternation, not only the error→exception
canonicalization.

The one miss is vocabulary marginality, not a counterexample: expiry
sits in 182 kept body-slots against timeout's 2,471 (same artifact,
presence rows over 2 × 752,352 slots) — this register's word for the
concept is timeout, so the joint event has almost no room, the exact
thin-marginals shape the in-hand title census taught.

## The pre-committed readings, applied

- **Unique targets.** No supported row's support is minted by a
  mega-target: toggle/flag spreads 13 crossings over 8 distinct
  targets, flag/boolean 33 over 30, undo/rollback 10 over 8,
  revert/rollback 15 over 7, error/exception 9,437 over 3,472
  (`bench/w/results/w2-so-census-2026-08-20.json`). The declaration's
  headline rule is satisfied with the counts in the open.
- **The markup-text variant.** The license is not an artifact of the
  cleaning rule: all five supported rows clear the floor on the
  markup surface too (25, 8, 11, 178, 7,624 — same artifact), and
  error/exception reads LOWER there than on prose (7,624 against
  9,437) — kept code spans inject the implementation form into
  closed-side surfaces and destroy exclusivity, the paste-direction
  bias declared confound 4 named, running against support exactly as
  stated. Support survived the bias on both surfaces.
- **timeout/expiry on the markup surface** reads 4 — under the floor
  on both surfaces; the miss is real, not a cleaning artifact.

## Stage 0, in its declared place

Before the fetch, the in-hand legacy title read
(`bench/w/results/w2-so-stage0-2026-08-20.json`): 40,103 pairs, park
band — error/exception 35 (25 on the closed side), everything else at
zero or one, library/package the expanded family's single support.
Beside the in-hand title census's zero of six at 37,436 pairs
(`bench/w/results/w2-pair-census-2026-08-20.json`), the thin surface
had already separated the registers; the body surface widened the
separation into the license.

## The expanded family, for the training declaration's benefit

21 of 45 supported (`bench/w/results/w2-so-census-2026-08-20.json`),
and no longer only the substitutability class the trained geometries
already carry: library/package 820 exclusive over 675 targets,
library/dependency 462, dependency/package 324, undo/revert 51,
rip/remove 43, old/legacy 33, remove/removal 31, hash/checksum 30,
bar/policy 29, credential/secret 22, dump/backup 22, backup/restore
20, rollback/revert 15, hash/sha256 13, dump/restore 13, hash/digest
9, bar/criteria 9, stage/phase 6, staged/stage 5, plus toggle/flag
and undo/rollback (which the imported sets carry in both blocks). The
same-side rows sit beside them in the artifact — error/exception
8,727, library/package 1,892, flag/boolean 136 — the co-occurring
raw material W1c's design fact says a W2 objective must also reward.

## Determinism

The census stage re-run from the derived file
(`bench/w/corpus/derived/w2-so-bodies-2026-08-20.tsv`, 1,958,801,633
bytes, sha256 3adb605e01601cde… recorded in
`bench/w/results/w2-so-census-2026-08-20.json`) reproduced the counts
block identically, directional and unique-target rows included. The
CI leg (`tests/test_w2_so_determinism.py`) pins the eager arrangement
equal to the imported reference on the committed fixture.

## What the license opens — and does not

OPENED: the W2 training declaration over the Stack Overflow body pair
set may now be written. It owns, before any run: the supervision-mass
argument on these counts, an objective answering W1c's design fact
(the same-side rows are in the artifact for exactly that argument),
the training-input licensing posture for the derived pairs (CC BY-SA
per-post attribution — the TSV stays uncommitted, its sha the
artifact's), and the full gate stack — determinism, no-damage, and
its class and horizon bars — under W_PROGRAM_DECLARATION §5 discipline
before anything touches the default engine.

NOT opened: no training result, no recall number, no default-engine
change. The preference routes stay closed — the continuity row's
strongest need reads 102 across 752,352 pairs
(`bench/w/results/w2-so-census-2026-08-20.json`). The in-hand parks
stand on their own surfaces and registers. Neither bar is restated by
this record. The tech-register column reads zero by construction on a
single-register corpus, as declared.

## The chain

Declaration e191d1a, reader with its CI leg 257046d, the stage 1 pin
47820d0, then the runs and this record — sha-ordered, one push. Lane
W's live unit is now the W2 training declaration; the standing owner
doors (both bar restatements, Lane T ratification) are unchanged, and
the publication plan's first gate — a gate-passing own-trained
artifact — now has a licensed data axis feeding it.
