# Harness fixture — NOT an instrument

The files here exist to test `run.py`. They are obviously-fake
placeholder content with no linguistic properties worth measuring, and
they are **never** part of a held-out instrument.

An instrument lives in `../data/` and is authored independently under
the seal protocol in `../FORMAT.md`. If you are looking for the
instrument and found this instead, you are in the wrong directory.
