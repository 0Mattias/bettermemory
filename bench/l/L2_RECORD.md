# L2 — the gate read and the park, 2026-08-16

The unit `bench/l/L2_DECLARATION.md` declared ran whole: anatomy
published, mechanisms implemented dark, four tuning reads walked the
declared space, finals committed, gate read taken. The verdict
grid's third row fires: **L2-PARK — G1h and G2b broken, the gain
under the partial floor.** The constants revert dark in this commit;
the default engine is behaviorally 6.1.0 and nothing ships. This
record publishes the read and what the park teaches; nothing below
renegotiates a bar.

## The gate grid

Gate configuration (commit `fabeb35`, read 4's): widening dropped
(min stems None, § "what tuning killed"), keyword scaffold weight
0.5 — the declared cap.

LongMemEval, lexical, full 500, depth 200
(`bench/l/results/gate-l2-lme-conv-a-2026-08-16.json`, determinism
repeat `-b`, paired control `gate-l2-lme-off-2026-08-16.json`):

- **lane-on: macro recall@1 0.5362, macro recall@5 0.9049, macro@10
  0.9526** — against the incumbent's committed 0.5339 / 0.9062 /
  0.9494 (`bench/l/results/gate-lme-conv-a-2026-08-16.json`): @1 up
  0.23, **@5 DOWN 0.13**, @10 up 0.32.
- **G1 — MISS**: 0.9049 against the 0.916 reference line.
- **G1h — MISS, and decisively**: the halves, computable from the
  per-question sidecars: tuning half 0.9093 — reproducing tuning
  read 4's even-half figure exactly — and **holdout half 0.9005**,
  against the incumbent's 0.9057 and the bar's 0.9107. The tuning
  half's gain inverted on the untouched half. This is the overfit
  clause firing as designed: the weight curve that rose
  monotonically into its cap on the even half was fitting that
  half's movers, not the mechanism's population.
- **G2 — PASS**: dev asked, unpadded, prefilter off, lane on:
  recall@1 35%, recall@5 60%
  (`bench/l/results/gate-l2-dev-conv-2026-08-16.json`) — the
  committed baseline, conceded nothing; the whole dev results block
  is byte-identical between lane-on and lane-off, the inertness the
  declaration declined to predict returning with the widening's
  death.
- **G2b — MISS**: multi-session 0.8588 against its 0.8663 guard.
  By type at @5, lane-on against incumbent: temporal-reasoning
  0.8703 vs 0.8675 (+0.28, n=133), **multi-session 0.8588 vs 0.8663
  (−0.75, n=133)**, and the other four types IDENTICAL to four
  decimals — single-session-assistant 1.0000 (n=56),
  knowledge-update 0.9808 (n=78), single-session-user 0.9714
  (n=70), single-session-preference 0.7333 (n=30). The saturated
  types and the preference type never moved once in six reads; the
  mechanisms' blast radius stayed exactly where aimed, and where
  aimed was a net loss.
- **G3 — PASS in every clause**: the gate's off arm reproduces the
  committed lane-off macros and by-type table exactly
  (0.5246/0.8935); the doubled lane-on invocation is identical to
  itself modulo wall-clock seconds; zero dependencies, no
  nondeterminism.

The five gate invocations ran back-to-back at one commit in one
sitting, per §5.

## What tuning killed, and what the gate killed

The read ledger (`bench/l/results/tune-l2-01` … `tune-l2-04`, each
with its config commit; four of the declared eight spent):

1. **The composed defaults are net-negative.** Read 1 (min stems 2,
   weight 0.0): tuning half 0.8999 against the incumbent's 0.9067,
   ten questions down against four up. The paired movers decompose
   the damage cleanly: seven of ten downs temporal-gated — the
   zero weight collapses a temporal gold's keyword-leg separation
   over generic content matchers into a raw/coverage tie, and the
   recency tiebreak hands the rank to the newer wrong doc, the
   inverse of the gold-is-earliest evidence — against three downs
   and two ups among the widening's scaffold-gated questions.
2. **The widening is dead, by its own confound.** The three
   scaffold-gated multi-session downs persisted at both weights
   (reads 1 and 2): in count asks the BODIES' scaffold — amounts,
   totals, year phrases — is evidence, and the widened df-floor
   strips the gold expense-narration's edge over content lookalikes
   that match the topic without the quantities. Declared confound 5
   priced itself: the flip thesis held only at the margins (two ups)
   and the risk pool was real. Dropped in read 3; min stems None is
   the drop.
3. **The keyword weight has no safe operating point.** The clean
   edge (temporal gate only, reads 3 and 4) walked the weight from
   0.25 to the 0.5 cap and rose — tuning half +0.07 then +0.27,
   with @1 positive and multi-session restored exactly. The gate
   then took the same config to the untouched half: 1 up, 3
   down, all temporal-gated, among them two multi-session elapsed
   asks losing every evidence session. The even-half curve was
   half-fitting. A temporal narration's scaffold co-match is
   load-bearing separation in the keyword leg — repricing it trades
   a rescue for losses at every weight this unit was allowed to
   price, and which questions land on which side is a property of
   the half, not the mechanism.
4. **The movers are few and the trade is a coin flip.** Six reads
   moved a dozen-odd questions each at most; every configuration
   bought its rescues with comparable losses. The L1 record's
   residual sentence — "the remaining gap is the keyword leg" — is
   now measured: the keyword leg's scaffold credit is not cheap
   headroom. It is half of a balance the fusion already struck.

## What the park licenses, exactly

Nothing ships. The constant pair reverts to None in this commit; the
dark-state test pins the reversion; the default engine is
behaviorally identical to 6.1.0 and the honest LongMemEval sentence
is unchanged: macro recall@5 90.6% against the reference stack's
91.6%. The criterion stays unclaimed, the interim sentence stands,
and no ship sentence is put — a park earns none.

What the park buys the program: Lane L's cheap deterministic route
to the reference line is exhausted — a published read, not an
assumption. The engine finds the evidence (macro@10 0.9526 lane-on,
`bench/l/results/gate-l2-lme-conv-a-2026-08-16.json`) and the
remaining @5 distance is now known to live where the standing
decomposition put it — the single-session-preference class
(embedding-shaped, 0.7333 in the same artifact against the
reference stack's own preference row) plus small
temporal/multi-session residuals that pricing repairs cannot reach
without meaning. That is Lane W's burden: W3-P, the
paraphrase-bridge unit, is the front of the queue on its own
merits now, not by default.

## Owner doors

None opened by this unit. The Lane T criterion-v1 ratification and
the standing interim sentence are where the last record left them.
